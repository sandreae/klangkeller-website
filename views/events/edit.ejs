<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Edit Event</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
      <div class="title">
        <h1>KLANGKELLER</h1>
        <h2>Edit Event</h2>
      </div>
      <form action="/events/update/<%= event._id %>" method="post" id="event">
        <table>
          <tbody>
            <tr>
              <td>KLANGKELLER ID</td>
              <td><input type="text" name="klangkellerID" required value="<%= event.klangkellerID %>"/></td>
            </tr>
            <tr>
              <td>Date (yyyy-mm-dd)</td>
              <td><input type="datetime" name="date" required value="<%= event.date %>"/></td>
            </tr>
            <tr>
              <td>Time</td>
              <td><input type="text" name="time" required value="<%= event.time %>"/></td>
            </tr>
            <tr>
              <td>Venue</td>
              <td>
                <select name="venue" form="event" required value="<%= event.venue %>">
                  <option value="kino">Kino</option>
                  <option value="keller">Keller</option>
                </select>
              </td>
            </tr>
            <tr>
              <td>Number of slots</td>
              <td><input type="number" name="slotNumber" min="1" max="10" required value="<%= event.slotNumber %>"/></td>
            </tr>
            <tr>
              <td>Maximum slot length</td>
              <td><input type="number" name="maxSlot" min="5" max="60" required value="<%= event.maxSlot %>"/></td>
            </tr>

            <tr>
              <td>Main organiser</td>
              <td>
                <select name="contact" form="event" required value="<%= event.contact %>">
                  <option value="Klaas">Klaas</option>
                  <option value="Mio">Mio</option>
                  <option value="Neo">Neo</option>
                  <option value="Sam">Sam</option>
                </select>
              </td>
            </tr>
            <tr>
              <td colspan="2"><button type="submit">Save</button></td>
            </tr>
          </tbody>
        </table>
      </form>
    </div>
  </body>
</html>
