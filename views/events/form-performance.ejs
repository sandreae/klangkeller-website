<!DOCTYPE html>
<html>
  <%- include('../partials/head', {options}) %>  
  <body>
    <div class="container">
      <%- include('../partials/title') %>        
      <h2><%= data.event.klangkellerID %> // <%= data.event.date.toDateString() %></h2>
  <div class="content">
      <form id="event" action="/events/performance/<%= data.event._id%>/<%= query.slot %>" method="post">
        <table>
          <tbody>
            <tr>
              <td>
                <div>
                  <label for="title"><h3>Title</h3>
                  <p>The title of your performance or your/your groups name.</p>
                  </label>
                </div>
                <input id="title" type="text" name="title" required/>
              </td>
              </tr>
              <tr>
                
                <td>
                  <div>
                    <label for="description">
                      <h3>Description</h3>
                      <p>This is the public information people will be able to see when they visit the website. You can list who will be performing and whatever info you want to provide. You can also link to other info with a url.
                      </p>                    
                    </label>
                  </div>
                  <textarea id="description" name="description" form="event" class="description"></textarea>
                </td>
              </tr>
              <tr>
                <td>
                  <div><label for="duration"><h3>Duration (in minutes)</h3></label></div>
                  <select id="duration" name="duration" form="event" required>
                    <%- include('../partials/construct-options') %>
                    <%- constructOptions( data.event.remainingSlotTimes ) %>
                  </select>
              </td>
              </tr>
              <tr>
                <td>
                  <div><label for="contact"><h3>Contact email</h3></label></div>
                  <input id="contact" type="email" name="contact" required/></td>
              </tr>
              <td colspan="2"><button type="submit">Save</button></td>
            </tr>
          </tbody>
        </table>
      </form>
    </div>
    <div class="about">
      <%- include('../partials/signup-info') %>
    </div>
    </div>
  </body>
</html>
