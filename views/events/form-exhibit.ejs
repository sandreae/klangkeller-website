<!DOCTYPE html>
<html>
  <%- include('../partials/head', {options}) %>  
  <% var eventDate = data.event.date.toDateString() %>
  <body>
    <div class="container">
      <%- include('../partials/title') %>        
      <h2><%= data.event.klangkellerID %> // <%= data.event.date.toDateString() %></h2>
    <div class="content">
      <form id="event" action="/events/exhibit/<%= data.event._id%>" method="post">
        <table>
          <tbody>
            <tr>
              <td><label for="title"><h3>Title</h3>
                <p>Your exhibition title.</p>
              </label>
            <input id="title" type="text" name="title" required/></td>
              </tr>
              <tr>
                <td>
                <label for="description"><h3>Description</h3>
                  <p>This is the public information people will be able to see when they visit the website. You can describe your exhibition and whatever other info you want to provide. You can also link to other info with a url.
                  </p>                    
                </label>
                <textarea id="description" name="description" form="event" class="description"></textarea>
                </td>
              </tr>
              <tr>
                <td>
                <div><label for="contact"><h3>Contact email</h3></label></div>
                <input id="contact" type="email" name="contact" required/>
              </td>
              </tr>
              <td colspan="2"><button type="submit">Save</button></td>
            </tr>
          </tbody>
        </table>
      </form>
    </div>
    <div class="about">
      <%- include('../partials/signup-info') %>
    </div>
    </div>
  </body>
</html>
