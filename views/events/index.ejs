<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>KLANGKELLER</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
        <script type="text/javascript" src="/js/test.js" crossorigin=""></script>

  </head>
  <body>
    <div class="container">
    <% include ../partials/title %>
      <div class="about">
      <% if(query.signup === "true") { %>
        <% include ../partials/signup-info %>
      <% } else { %>
        <% include ../partials/about %>
      <% } %>
    </div>
    <div class="content">
      <% include ./list %>
      <% if(query.admin === "true") { %>
        <a href="/events/create">Create Event</a>
      <% } %>
    </div>
    </div>
  </body>
</html>
